# GPT Tools MCP

让 ChatGPT 通过一个 MCP 连接器操作多个本地项目。

GPT Tools MCP 是基于 Tauri 2、Rust、Svelte 5 和 TypeScript 的桌面应用。桌面端统一管理 MCP 服务、Cloudflare 隧道和认证；添加本地目录作为工作区任务后，多个 ChatGPT 对话可以分别绑定不同任务，使用文件、Git、终端和项目历史工具。

- 源码：https://github.com/resign13/GPT-Tools-MCP
- 发布版本：https://github.com/resign13/GPT-Tools-MCP/releases
- 当前源码版本：`0.3.0`。安装包所含功能以对应发布版本为准。

## 功能概览

| 功能 | 说明 |
| --- | --- |
| 单 MCP、多任务 | 共用一个公网入口和连接器，任务目录独立管理 |
| 对话绑定 | 使用任务 ID、名称或路径绑定当前对话，成功后锁定目标 |
| 并发操作 | 多个对话可请求执行任务；网关协调同一目录的读写请求 |
| 文件与代码 | 列目录、读取、搜索、查看图片、应用 Patch |
| 命令会话 | 执行命令、读取输出、发送输入、结束命令会话 |
| Git | 查看状态、差异、日志、提交内容和 blame |
| 项目历史 | 初始化、恢复、检索和保存项目会话进度 |
| 工作上下文锁 | 对 Git worktree 固定执行根目录，并校验分支等上下文 |
| Cloudflare | 支持临时 Quick Tunnel 和长期 Named Tunnel |
| OAuth | 通过桌面端管理连接器认证配置 |

实际可调用工具取决于当前版本、工具 profile 和任务策略。FRP 已弃用，本文统一使用 Cloudflare，旧配置中的 FRP 入口属于兼容保留内容。

## 工作方式

```text
ChatGPT 对话 A ── 绑定任务 A ─┐
                            ├── 同一个 HTTPS /mcp ── Cloudflare Tunnel
ChatGPT 对话 B ── 绑定任务 B ─┘                            │
                                                     本地 MCP 网关
                                                        │
                                              ┌─────────┴─────────┐
                                           任务 A 目录         任务 B 目录
```

只需配置一次宿主 MCP、OAuth 和隧道。新建工作区任务不会启动第二个 MCP，也无需为每个任务新增域名或连接器。

桌面端的“当前任务”用于管理任务；每个网页对话的操作目标由该对话自己的绑定决定。切换桌面选中项不会替其他对话切换项目。

## 安装和启动

### 使用安装包

1. 打开 Releases 页面，选择与操作系统匹配的安装包。
2. 安装并启动桌面应用。
3. 按下文配置 MCP 和 Cloudflare。

### 从源码启动

准备 Node.js 20 或更新版本、npm、Rust stable，以及 Tauri 2 对应的系统依赖。Windows 开发环境需要 Microsoft C++ Build Tools 和 WebView2。

```powershell
git clone https://github.com/resign13/GPT-Tools-MCP.git
cd GPT-Tools-MCP
npm install
npm run desktop
```

`npm run desktop` 启动完整桌面开发环境，默认关闭源码自动监听。需要监听变化时使用：

```powershell
npm run desktop:watch
```

`npm run dev` 仅启动前端开发服务器，不等同于完整桌面 MCP 服务。

## 第一步：配置本地 MCP 与工作区任务

1. 打开桌面端的网关管理页面，设置宿主 MCP。
2. 确认监听端口，例如 `28766`；后续所有示例均以此端口说明。
3. 在认证配置中启用 OAuth，准备界面提供的 Client ID、Client Secret 和授权口令。
4. 保存配置并启动 MCP。
5. 在“工作区任务”中新建任务，选择实际项目目录。
6. 复制任务 ID，供网页对话绑定使用。

在网关任务界面创建的任务会自动加入可绑定列表。删除任务只移除配置，不会删除对应本地目录。首次验证建议准备两个不同目录的任务。

如果沿用旧版独立工作区配置，需要确认已启用网关且目标任务在可访问列表内。

## 第二步：配置 Cloudflare 公网入口

### 选择哪种模式

| 模式 | 域名要求 | 地址特点 | 适用场景 |
| --- | --- | --- | --- |
| Named Tunnel | 自有域名已在 Cloudflare 激活 | 固定 HTTPS 地址 | 长期使用，推荐 |
| Quick Tunnel | 不需要自有域名 | 临时地址，重启可能变化 | 首次体验、临时测试 |

两种模式都不需要额外购买 VPS，也不要求家庭网络具有公网 IP。本地电脑、桌面 MCP 服务和隧道需要保持运行；电脑关机或休眠时远程调用会中断。

### 方案 A：Named Tunnel，长期使用

#### 1. 接入域名

1. 在 Cloudflare 添加域名。
2. 在域名注册商处，将 Nameserver 修改为 Cloudflare 为该域名分配的两个地址。
3. 等待 Cloudflare 域名状态显示 `Active`。
4. 为 MCP 选择独立子域名，例如 `mcp.example.com`。

已有网站可以继续使用其他主机名。为 MCP 创建新子域名即可，无需修改网站原有记录。

#### 2. 创建隧道并取得 Token

1. 进入 Cloudflare Zero Trust 控制台。
2. 在网络相关菜单中找到 Tunnels；不同界面可能显示在 Networks、Connectors 等入口下。
3. 创建隧道，连接器类型选择 `cloudflared`。
4. 为隧道命名，例如 `gpt-tools-mcp`。
5. 在安装连接器页面找到运行命令，复制其中的 Tunnel Token。

命令一般包含类似 `--token <TOKEN>` 或 `service install <TOKEN>` 的内容。桌面 Token 字段仅填 Token 本身，不粘贴整条命令。由桌面端管理隧道时，按桌面流程启动即可。

#### 3. 填写桌面端隧道配置

| 配置项 | 填写内容 |
| --- | --- |
| 隧道类型 | Cloudflare |
| 模式 | Named Tunnel |
| Tunnel Token | 上一步取得的 Token |
| 公网 URL | `https://mcp.example.com` |
| 网络代理 | 按本机网络情况选择；启用时使用全局代理配置 |

点击“保存配置”，再启动隧道。回到 Cloudflare，确认连接器在线后继续设置公开路由。

公网 URL 填根地址，不附加 `/mcp`。应用会为 MCP 和 OAuth 等接口分别生成完整地址。保存后配置和 Token 由应用持久化，下次使用同一应用配置无需重复填写。

#### 4. 配置公开主机名

在该隧道的 Public Hostname 或 Published application routes 页面添加：

| 字段 | 示例 |
| --- | --- |
| Subdomain | `mcp` |
| Domain | `example.com` |
| Path | 留空 |
| Service Type | HTTP |
| Service URL | `127.0.0.1:28766` |

保存路由后，最终 MCP 地址为：

```text
https://mcp.example.com/mcp
```

本地端口应与桌面 MCP 的实际监听端口一致。Path 留空，让 MCP、OAuth 授权和元数据路由都到达同一个本地服务。外部使用 HTTPS，本地服务使用 HTTP。

如果 Cloudflare 提示 DNS 记录冲突，先核对新子域名对应的旧记录，再处理该主机名的冲突；已有网站记录保持原用途。

#### 5. 验证连接

1. 桌面 MCP 显示运行中。
2. 桌面隧道显示运行中，Cloudflare 连接器在线。
3. 运行应用内连接测试或健康检查。
4. 检查公网 MCP、OAuth 元数据和受保护资源发现接口。
5. 从桌面端复制完整公网 MCP 地址，进入下一步。

浏览器直接访问 `/mcp` 并不等同于一次 MCP 工具调用；看到认证或协议相关响应时，应结合连接器和健康检查判断状态。

### 方案 B：Quick Tunnel，临时体验

1. 启动本地 MCP。
2. 在隧道配置中选择 Cloudflare → Quick Tunnel。
3. 保存并启动隧道。
4. 等待应用显示临时 HTTPS 地址，复制应用生成的完整 MCP URL。
5. 在 ChatGPT 配置连接器并授权。

临时地址变化后，需要更新连接器及相关公网地址配置，并重新授权。长期使用建议改用 Named Tunnel。

## 第三步：连接 ChatGPT 网页端

1. 打开 ChatGPT 设置中的应用或连接器管理入口。
2. 在支持自定义 MCP 的账户环境中启用相应开发者选项。菜单名称以当前网页界面为准。
3. 创建自定义连接器，名称可填写 `GPT Tools MCP`。
4. MCP URL 填写 `https://mcp.example.com/mcp`。
5. 身份验证选择 OAuth。
6. 如界面要求高级 OAuth 凭据，填写桌面端提供的 Client ID 和 Client Secret。项目使用预配置客户端，不依赖 CIMD 配置。
7. 完成连接流程；在弹出的本项目授权页面输入桌面端提供的授权口令。
8. 新建聊天并启用该连接器。

三种凭据用途不同：

| 凭据 | 使用位置 |
| --- | --- |
| Cloudflare Tunnel Token | 桌面端 Cloudflare 隧道配置 |
| OAuth Client ID / Client Secret | ChatGPT 连接器 OAuth 配置 |
| 授权口令 | 本项目的 OAuth 授权页面 |

登录 ChatGPT 账号与授权访问本地 MCP 是两个步骤。无需把以上凭据发送到聊天中。

## 第四步：绑定工作区任务

### 推荐：使用任务 ID

复制下面提示词，将 `TASK_ID` 替换为桌面端复制的实际任务 ID：

```text
使用任务 TASK_ID。
请先调用 bind_workspace，workspace_hint 填写这个任务 ID。
绑定成功后调用 get_selected_workspace 和 get_default_cwd，确认任务和目录。
然后调用 history_session_bootstrap，将我的这条首次请求逐字传入 initial_user_input。
本对话后续操作都在这个任务中执行。先读取项目目录并汇报项目结构。
```

也可以使用准确名称或路径：

```text
使用 my-project，先绑定对应工作区任务，再读取当前项目目录。
```

工具仅匹配已允许访问的任务。名称重复时需根据候选列表改用 ID；未指定任务时，应先列出任务并选择，不会默认操作宿主目录。

### 两个对话同时使用

1. 桌面端添加任务 A、任务 B，复制各自 ID。
2. 网页新建对话 A，启用连接器并绑定任务 A。
3. 网页新建对话 B，启用同一个连接器并绑定任务 B。
4. 分别调用 `get_default_cwd`，确认返回各自目录。
5. 分别提出任务，两条对话即可请求并行执行。

同一对话绑定成功后保持锁定，切换任务请新建对话。应用或 MCP 重启后，内存中的绑定需要重新建立；项目历史档案仍可通过历史工具恢复。

任务绑定和命令会话按对话隔离。不同任务若指向同一物理目录，仍共享该目录的文件，建议用独立目录或 Git worktree 区分并行开发任务。

## 历史会话与持续推进

首次绑定后，通过 `history_session_bootstrap` 初始化或恢复项目历史。默认恢复有界状态，需要细节时再调用 `history_session_search` 和 `history_session_read` 分页读取。

可追加以下提示词：

```text
每轮任务完成后调用 history_session_checkpoint。
使用 bootstrap 返回的 session_key 和 current_path，并按工具要求填写 expected_path。
将本轮用户请求逐字传入 raw_user_input。
只有工具返回保存成功且目标一致后，才报告进度已保存。
需要早期细节时先搜索历史，再按需分页读取。
```

历史档案位于项目的 `docs/history-session/`。服务端保存工具传入的内容，不会自动录制网页全部聊天记录。

MCP 提供执行工具，不是独立的永久后台代理。模型是否继续调用工具仍由当前对话驱动；长命令返回会话标识后，需要继续读取输出确认完成。

## Git worktree 上下文固定

对于工具目录中提供上下文锁能力的版本，可在任务绑定后固定一个已存在的 Git worktree，降低工作目录与分支不一致的问题。

示例工具调用参数：

```json
{
  "path": "D:\\projects\\my-project\\worktrees\\feature-demo",
  "expected_branch": "feature/demo",
  "expires_in_minutes": 120
}
```

以上参数传给 `pin_workspace_context`；示例 worktree 应已创建且位于所选任务允许的目录范围内。随后调用 `get_workspace_context` 检查执行根目录、分支与锁状态。

完成阶段性修改后，结合 `git_status`、`git_diff` 和文件读取验证结果。收到上下文不匹配错误时，先核对实际目录和分支，再继续操作。

## 常见问题

| 现象 | 排查方式 |
| --- | --- |
| Cloudflare 域名一直未激活 | 核对注册商 Nameserver 是否与 Cloudflare 分配值一致；传播提示不等于 Active |
| 隧道在线但返回 502 | 核对本地 MCP 是否运行、路由是否为 HTTP、端口是否正确 |
| 公网地址出现 404 | 核对公开主机名、路由 Path 和连接器 URL；MCP 地址应以 `/mcp` 结尾 |
| OAuth 失败 | 检查公网根地址、OAuth 元数据及 Client ID/Secret 是否与当前宿主一致 |
| 跳到额外登录或验证页面 | 检查该主机名是否叠加了 Cloudflare Access 或交互挑战，并核对连接器兼容性 |
| `workspace_not_selected` | 先调用 `bind_workspace`，再调用项目工具 |
| `workspace_not_found` | 核对任务 ID、任务是否存在及是否在可访问列表内 |
| `workspace_ambiguous` | 多个任务名称或目录名重复，改用唯一任务 ID |
| `workspace_locked` | 当前对话已绑定另一任务，新建对话绑定目标任务 |
| `workspace_changed` | 目录或策略变化导致旧绑定失效，按返回提示重新建立绑定 |
| `session_required` / `session_identity_conflict` | 检查客户端会话标识及连接器版本，重新连接并使用新对话验证 |
| `gateway_busy` | 请求正在排队或达到并发限制，稍后重试 |
| 网页工具列表与桌面不一致 | 确认运行的是最新构建，刷新连接器工具目录，必要时重新连接 |
| 模型执行一段后停止 | 检查上一条工具结果是否要求选择、授权、补充参数或读取长命令输出 |
| 重开应用后 Token 未恢复 | 确认此前保存成功、使用相同应用配置与系统账号，并检查凭据存储错误 |

新增普通任务不需要修改 DNS。变更宿主端口、OAuth 或工具目录后，按应用提示重启 MCP，并检查 Cloudflare 路由及连接器配置是否仍对应。

## 开发与构建

```powershell
npm run check
npm run build
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
```

构建桌面安装包：

```powershell
npm run desktop:build
```

主要目录：

```text
src/                         Svelte 界面与前端状态
src-tauri/src/mcp/           MCP 协议、网关与会话路由
src-tauri/src/tools/         文件、Git、命令等工具
src-tauri/src/workspace/     工作区配置模型
src-tauri/src/security/      权限与执行策略
src-tauri/src/harness/       项目执行状态与记录
docs/                       设计、规格与配置文档
```

GPT Actions 使用独立的 OpenAPI 接口，保持单工作区模式；本文多对话、多任务绑定流程面向 MCP 连接器。

## 许可证

项目许可证声明为 Apache-2.0。分发时保留适用的许可证及版权声明。
